# Тяжёлые ML зависимости — ставить только если VLM_ENGINE=transformers локально
# 526 MB torch + 11 MB transformers — не нужно для mock / openai / vllm API
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.44.2
accelerate>=0.33.0
sentence-transformers>=3.0.1
easyocr>=1.7.1
paddleocr>=2.9.1
open-clip-torch>=2.24.0
